git---


git config --global user.name "name"
git config --global user.email "email"

init git ripo:
    git init


git status --> check changes

git add "file" --> add file to be comitted "git add ." add al files wich were modyfied or createt
git rm "file" -_> remove treckt file

git commit "file" -m "message" --> commiting files
git commit "file" --> commit + open editor to edit meaasage --> need to close file in editor

git log -->  history of commits

git checkout "hash" --> hash from got log --> checkout past commit
git checkout master --> back to master branch

git branch --> list all branch
git branch "new branch name" --> create new branch

git merge "branchname" merge branch with master

git push --> take local ripo and push to global/internet ripo (like github)
git pull --> gets global/internet ripo
git clone --> clones a ripo from the web

touch .gitignore
    wirite all file names and dir's in that wont be checkt and synced ("dir: /[dirname]")

files : * funktioniert


----------
git hub

create new
echo "# TestRipo" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/supertoll/TestRipo.git
git push -u origin main

push existing
git remote add origin https://github.com/supertoll/TestRipo.git
git branch -M main
git push -u origin main



git push to update global ripo
git pull to get global ripo